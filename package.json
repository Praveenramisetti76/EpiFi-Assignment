{
  "name": "fair-split",
  "version": "1.0.0",
  "description": "A restaurant bill splitter that takes a photo of a receipt and a plain-English description and returns a reconciled per-person breakdown.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node test.js"
  },
  "dependencies": {
    "@google/genai": "^2.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
